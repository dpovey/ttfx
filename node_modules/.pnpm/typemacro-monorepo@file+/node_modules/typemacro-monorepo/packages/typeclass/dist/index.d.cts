import * as _typemacro_core from '@typemacro/core';
import * as ts from 'typescript';

interface TypeclassInfo {
    name: string;
    methods: Array<{
        name: string;
        typeParams: string[];
        params: Array<{
            name: string;
            type: string;
        }>;
        returnType: string;
    }>;
}
interface InstanceInfo {
    typeclass: string;
    forType: string;
    expression: ts.Expression;
}
/**
 * Clear registries (for testing)
 */
declare function clearRegistries(): void;
/**
 * Get all registered typeclasses
 */
declare function getTypeclasses(): Map<string, TypeclassInfo>;
/**
 * Get all registered instances
 */
declare function getInstances(): Map<string, InstanceInfo>;
declare const typeclassAttribute: _typemacro_core.AttributeMacro;
declare const instanceAttribute: _typemacro_core.AttributeMacro;
declare const derivingAttribute: _typemacro_core.AttributeMacro;
declare const summonMacro: _typemacro_core.ExpressionMacro;
declare const extendMacro: _typemacro_core.ExpressionMacro;
/**
 * Find an extension method for a type and method name.
 * Used by the transformer to rewrite x.show() -> showInstance.show(x)
 */
declare function findExtensionMethod(typeName: string, methodName: string): {
    instance: InstanceInfo;
    typeclass: TypeclassInfo;
} | undefined;
/**
 * Register macros with the global registry.
 * Call this function to enable typeclass macros in your project.
 */
declare function register(): void;

export { clearRegistries, derivingAttribute, extendMacro, findExtensionMethod, getInstances, getTypeclasses, instanceAttribute, register, summonMacro, typeclassAttribute };
