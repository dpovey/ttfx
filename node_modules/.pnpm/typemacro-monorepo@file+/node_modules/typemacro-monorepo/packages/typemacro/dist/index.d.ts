export * from '@typemacro/core';
import * as comptime from '@typemacro/comptime';
export { comptime };
import * as reflect from '@typemacro/reflect';
export { reflect };
import * as derive from '@typemacro/derive';
export { derive };
import * as operators from '@typemacro/operators';
export { operators };
import * as typeclass from '@typemacro/typeclass';
export { typeclass };
import * as specialize from '@typemacro/specialize';
export { specialize };

/**
 * typemacro - TypeScript Compile-Time Macros
 *
 * Scala 3-style metaprogramming for TypeScript. Write macros that transform
 * code at compile time, eliminating runtime overhead while maintaining
 * type safety.
 *
 * ## Quick Start
 *
 * ```ts
 * // Use built-in macros
 * import { derive, ops, pipe, compose } from "typemacro";
 *
 * // Derive pattern implementations at compile time
 * @derive(Eq, Debug, Clone)
 * class User {
 *   constructor(public id: number, public name: string) {}
 * }
 *
 * // Operator overloading
 * @operators
 * class Vec2 {
 *   constructor(public x: number, public y: number) {}
 *   add(other: Vec2) { return new Vec2(this.x + other.x, this.y + other.y); }
 * }
 * const result = ops(v1 + v2); // Compiles to: v1.add(v2)
 *
 * // Function composition
 * const process = pipe(
 *   data,
 *   parse,
 *   validate,
 *   transform
 * ); // Compiles to: transform(validate(parse(data)))
 * ```
 *
 * ## Bundler Integration
 *
 * ```ts
 * // vite.config.ts
 * import typemacro from "typemacro/vite";
 *
 * export default {
 *   plugins: [typemacro()],
 * };
 * ```
 *
 * @module
 */

/**
 * Register all built-in macros with the global registry.
 * Call this at the start of your build to enable all built-in macros.
 */
declare function registerAllMacros(): void;

export { registerAllMacros };
