import * as _typemacro_core from '@typemacro/core';

/**
 * Compile-Time Reflection Macros
 *
 * Provides compile-time type introspection capabilities, allowing you to
 * examine types, generate code based on type information, and more.
 *
 * Inspired by:
 * - Rust's proc_macro with derive
 * - Java/C# reflection but at compile time
 * - Zig's @typeInfo
 *
 * @example
 * ```typescript
 * @reflect
 * interface User {
 *   id: number;
 *   name: string;
 *   email: string;
 * }
 *
 * // Get metadata at compile time
 * const userMeta = typeInfo<User>();
 * // { name: "User", fields: [{ name: "id", type: "number" }, ...] }
 *
 * // Generate a validator
 * const validateUser = validator<User>();
 * ```
 */
interface TypeInfo {
    name: string;
    kind: "interface" | "class" | "type" | "enum" | "primitive" | "union" | "intersection" | "array" | "tuple" | "function";
    fields?: FieldInfo[];
    methods?: MethodInfo[];
    typeParameters?: string[];
    extends?: string[];
    modifiers?: string[];
}
interface FieldInfo {
    name: string;
    type: string;
    optional: boolean;
    readonly: boolean;
    defaultValue?: string;
}
interface MethodInfo {
    name: string;
    parameters: ParameterInfo[];
    returnType: string;
    isAsync: boolean;
    isStatic: boolean;
}
interface ParameterInfo {
    name: string;
    type: string;
    optional: boolean;
    defaultValue?: string;
}
declare const reflectAttribute: _typemacro_core.AttributeMacro;
declare const typeInfoMacro: _typemacro_core.ExpressionMacro;
declare const fieldNamesMacro: _typemacro_core.ExpressionMacro;
declare const validatorMacro: _typemacro_core.ExpressionMacro;
/**
 * Register macros with the global registry.
 * Call this function to enable reflection macros in your project.
 */
declare function register(): void;
type ValidationResult<T> = {
    success: true;
    value: T;
} | {
    success: false;
    errors: string[];
};

export { type FieldInfo, type MethodInfo, type ParameterInfo, type TypeInfo, type ValidationResult, fieldNamesMacro, reflectAttribute, register, typeInfoMacro, validatorMacro };
